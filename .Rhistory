MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
for (i<r){
SampleMyData<-sample(MyData,size=s, function),replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SeampleMeans, SampleMyData)
i<i+1
}
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
for (i<r){
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SeampleMeans, SampleMyData)
i<i+1
}
hist(SampleMeans)
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
for (i<r){
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<i+1
}
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
for (i<r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(100,20,3)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
MyData<-rnorm(100,20,10)
histogram(MyData
histogram(MyData)
hist(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-100
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(500,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
MyData<-rnorm(20,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
MyData<-rexp(20,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
MyData<-rexp(100,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
MyData<-rexp(100,20,10)
hist(MyData)
MyData<-rexp(100)
MyData
hist(MyData)
MyData<-rexp(1000)
hist(MyData)
MyData<-rexp(20)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
MyData<-rnorm(30,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans)
mean(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 20 observations, with a mean of 20, and standard deviation of 3
#Smaller data sets end up with lumpier looking histograms, while larger sets are closer to what is expected
MyData<-rnorm(1000,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
#The sample means from our resampled, larger set moves into a much more well defined version of the
#normal distribution and cleans up the imperfections.
hist(SampleMeans)
#define a set of data that we are going to bootstrap from
#I'm going to make it 100 observations, with a mean of 20, and standard deviation of 3
mean(SampleMeans)
sd(SampleMeans)
MyData<-rnorm(10000,20,10)
hist(MyData)
MeanMyData<-mean(MyData)
StdErrorMyData<-sd(MyData)/sqrt(length(MyData))
SampleVarianceMyData<-var(MyData)
sd(MyData)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans<-c(SampleMeans, mean(SampleMyData))
i<-i+1
}
#The sample means from our resampled, larger set moves into a much more well defined version of the
#normal distribution and cleans up the imperfections.
hist(SampleMeans)
mean(SampleMeans)
hist(SampleMeans)
hist(SampleMeans)
mean(SampleMeans)
sd(SampleMeans)
```{r the loop function 2}
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans2<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans2<-c(SampleMeans2, mean(SampleMyData))
i<-i+1
}
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans2<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans2<-c(SampleMeans2, mean(SampleMyData))
i<-i+1
}
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyData)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeans2<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyData<-sample(MyData,size = s,replace=TRUE)
#append SampleMyData to all of the SampleMeans
SampleMeans2<-c(SampleMeans2, mean(SampleMyData))
i<-i+1
}
hist(SampleMeans2)
mean(SampleMeans2)
sd(SampleMeans2)
hist(SampleMeans2)
mean(SampleMeans2)
sd(SampleMeans2)
MyData2<-rnorm(500,20,10)
hist(MyData2)
hist(SampleMeans)
mean(SampleMeans)
mean(MyData)
sd(SampleMeans)
sd(MyData)
hist(SampleMeans)
mean(SampleMeans)
mean(MyData)
sd(SampleMeans)
sd(MyData)/sqrt(length(MyData))
hist(SampleMeansTwo)
mean(SampleMeansTwo)
mean(MyDataTwo)
sd(SampleMeansTwo)
sd(MyDataTwo)/sqrt(length(MyDataTwo))
MyDataThree<-rexp(15)
hist(MyDataThree)
MeanMyDataThree<-mean(MyDataThree)
StdErrorMyDataThree<-sd(MyDataThree)/sqrt(length(MyDataThree))
SampleVarianceMyDataThree<-var(MyDataThree)
sd(MyDataThree)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyDataThree)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeansThree<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyDataThree<-sample(MyDataThree,size = s,replace=TRUE)
#append SampleMyDataThree to all of the SampleMeans
SampleMeans<-c(SampleMeansThree, mean(SampleMyDataThree))
i<-i+1
}
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyDataThree)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeansThree<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyDataThree<-sample(MyDataThree,size = s,replace=TRUE)
#append SampleMyDataThree to all of the SampleMeans
SampleMeansThree<-c(SampleMeansThree, mean(SampleMyDataThree))
i<-i+1
}
hist(SampleMeansThree)
#r is the repititions we will run
r<-1000
#s is the size of the sample we will take
s<-length(MyDataThree)
#i is a counter for the loop
i<-1
#place to store the means
SampleMeansThree<-c()
#loop to repetitively take a sample
while(i<=r)
{
SampleMyDataThree<-sample(MyDataThree,size = s,replace=TRUE)
#append SampleMyDataThree to all of the SampleMeans
SampleMeansThree<-c(SampleMeansThree, mean(SampleMyDataThree))
i<-i+1
}
hist(SampleMeansThree)
mean(SampleMeansThree)
mean(myDataThree)
sd(SampleMeansThree)
sd(myDataThree)/sqrt(length(MyDataThree))
mean(SampleMeansThree)
mean(MyDataThree)
sd(SampleMeansThree)
sd(MyDataThree)/sqrt(length(MyDataThree))
mean(SampleMeansThree)
mean(MyDataThree)
sd(SampleMeansThree)
sd(MyDataThree)/sqrt(length(MyDataThree))
hist(MyDataFour)
MeanMyDataFour<-mean(MyDataFour)
StdErrorMyDataFour<-sd(MyDataFour)/sqrt(length(MyDataFour))
SampleVarianceMyDataFour<-var(MyDataFour)
sd(MyDataFour)
hist(MyDataFour)
MeanMyDataFour<-mean(MyDataFour)
StdErrorMyDataFour<-sd(MyDataFour)/sqrt(length(MyDataFour))
SampleVarianceMyDataFour<-var(MyDataFour)
sd(MyDataFour)
getwd()
getwd()
setwd("~/R/Bootstrap")
